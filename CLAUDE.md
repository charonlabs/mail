# MAIL v2 Development Notes

This file is intentionally short. The detailed MAIL v1 onboarding notes were
archived at `src/mail/legacy/CLAUDE.v1.md`.

For current repository guidance, use `AGENTS.md` and the package-local docs:

- `src/mail/protocol`
- `src/mail/server`
- `src/mail/client`
- `src/mail/daemon`
- `src/mail/legacy`

New work should target the v2 packages unless the task explicitly concerns the
legacy runtime.

