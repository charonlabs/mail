# Obsolete Tests

These files describe API shapes that no longer match either the active MAIL v2
packages or the archived MAIL v1 runtime tests.

They are preserved for reference while the repository is reorganized, but their
filenames intentionally do not match `test_*.py` so pytest does not collect
them by default.

